[project]
name = "iscn-validator-api"
version = "1.0.0"
description = "ISCN Karyotype Validator API"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.104.0",
    "uvicorn[standard]>=0.24.0",
    "pydantic>=2.0.0",
]

[project.scripts]
serve = "uvicorn api.server:app"
